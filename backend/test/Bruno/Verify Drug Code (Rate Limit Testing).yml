info:
  name: Verify Drug Code (Rate Limit Testing)
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/v1/verify
  body:
    type: json
    data: |
      {
        "code": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
