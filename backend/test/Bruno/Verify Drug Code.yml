info:
  name: Verify Drug Code
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/v1/verify
  body:
    type: json
    data: |
      {
        "gtin": "03585554088980",
        "serial_number": "01590553843587",
        "batch_number": "B0250"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
