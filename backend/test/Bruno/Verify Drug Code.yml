info:
  name: Verify Drug Code
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/v1/verify
  body:
    type: json
    data: |
      {
        "code": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
